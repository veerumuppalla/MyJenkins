pipeline {
  agent any
  stages {
    stage('SCM Checkout') {
      steps {
        sh 'ls -la'
        sh 'echo $PWD'
      }
    }
    
    stage('Build') {
      steps {
        sh 'ls -la'
        sh 'echo bulding artefacts'
      }
    }
    
  }
}
